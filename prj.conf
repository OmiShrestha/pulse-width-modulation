# Enable logging in the application
CONFIG_LOG=y

# Enable support for PWM (Pulse Width Modulation) devices
CONFIG_PWM=y

# Enable support for LED drivers
CONFIG_LED=y

# Enable immediate logging mode for real-time log output
CONFIG_LOG_MODE_IMMEDIATE=y
